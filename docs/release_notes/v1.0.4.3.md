# v1.0.4.3 - cmdb-relationship-owner-count-menu

## Changes

- Changed the CMDB relationship coverage owner column to show only a fixed short label, such as `owner 4`.
- The full owner list remains available inside the row dropdown.
- This avoids row expansion when the first owner name is long.

## Verification

- `python -m pytest tests/test_asset_account_ui.py::test_cmdb_relationship_dashboard_contracts_exist tests/test_asset_account_ui.py::test_cmdb_relationship_page_renders_with_overview -q`
- `python -m pytest -q`

## Field Note

This patch changes only the CMDB relationship coverage UI. It does not modify CMDB data, relationship data, or topology calculations.
