# v1.0.3.99 - relationship-coverage-full-asset-scope

## Changes

- Fixed CMDB relationship coverage detail so it reads all host list pages instead of only the first 100 rows.
- Relationship coverage detail now includes every system/group row, making the detail table reconcile with the current asset scope.
- Added regression coverage for assets above the 100-row page limit.

## Verification

- `python -m pytest tests/test_asset_account_ui.py tests/test_ui_contracts.py -q`
- `python -m pytest -q`

## Field Note

If the asset total is 492, this page should calculate relationship coverage from all 492 non-retired visible assets, not only the first page.
