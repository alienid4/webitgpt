# v1.0.3.9 - 核心影響通知名單匯出

Build time: 2026-05-26 08:15:00 +08:00
Patch ID: core-impact-notification-export

## 變更內容

- 核心系統影響圖新增三欄背景與更明確欄名，讓「六大核心 / 關聯系統 / 主機 IP」掃讀更清楚。
- 右側影響面板新增待通知對象摘要，列出系統 owner、主機數與聯絡狀態。
- 新增 `/api/dependencies/notifications.csv`，可下載核心影響通知名單 CSV。
- 將頁首與 `/health` 版本更新為 `v1.0.3.9 / core-impact-notification-export`。

## 驗證方式

```bash
python -m py_compile webapp/config.py webapp/routes/api_reports.py webapp/services/dependency_service.py
python -m pytest tests/test_ui_contracts.py -q
python -m pytest -q
```

## 已知風險

- CSV 依 CMDB 系統 owner 欄位輸出；若 owner 未填，會標示為「未指定」並提示補聯絡人。
