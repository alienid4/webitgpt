# v1.0.3.89 topology-host-alias-backfill

## 重點

- 核心系統影響圖補齊主機時，改用第二層系統的 `display_name`、`metadata.asset_name`、`metadata.system_name` 與主機所有系統候選欄位比對。
- 修正同一套系統在 CMDB 欄位落點不一致時，拓撲只顯示 `dependency_systems.host_refs` 內部分主機的問題。
- 延續系統名稱正規化，`證券 / 證卷 / 証券 / 証卷` 會用同一口徑比對。

## 驗證

- 新增回歸測試：當主機 `system_name` 與第二層系統不同，但 `asset_name` / `device_type` 屬於同一套系統時，核心系統影響圖仍補齊主機。
- `python -m pytest tests/test_app.py::test_core_impact_backfills_hosts_when_system_name_field_differs -q`
- `python -m pytest tests/test_app.py tests/test_asset_account_ui.py -q`
- `python -m pytest -q`
