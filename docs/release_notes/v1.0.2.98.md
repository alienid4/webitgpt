# v1.0.2.98 - 完整離線安裝修正版

Build time: 2026-05-22 22:52:46 +08:00
Patch ID: offline-install-lf-idempotent

## 變更內容

- 修復完整離線包在 RHEL/Rocky 9 目標機執行時，內層 `INSTALL.sh` 因 Windows 換行造成失敗的問題。
- `INSTALL_ALL.sh` 解開 prerequisite/app 子包後，會自動把所有 `.sh` 正規化成 Linux LF 格式。
- app 離線子包打包時，也會先把所有 `.sh` 正規化成 Linux LF 格式。
- MongoDB container 已存在時，安裝流程會改成啟動與確認，不會要求工讀生手動處理。
- 版本資訊更新為 `v1.0.2.98`，用於新主機第一次完整離線安裝。

## 驗證結果

- Python 測試：`22 passed`
- 221 Linux bash 語法檢查：通過
- 完整離線包內容檢查：未包含 `data/`、`logs/`、`backup/`、`venv/`、`.git/`、Mongo dump
- 完整離線包 shell 換行檢查：`LF_OK`
- SHA256：

```text
40b1d545f320783a678c29b4331a1bdb39f1d0dadb985d3b5227c91eb4f286a0  webitgpt_full_offline_1.0.2.98-offline-install-lf-idempotent_20260522225921.tar.gz
```

## 給測試機的指令

```bash
tar -xzf webitgpt_full_offline_1.0.2.98-offline-install-lf-idempotent_20260522225921.tar.gz
cd webitgpt_full_offline_1.0.2.98-offline-install-lf-idempotent_20260522225921
sudo bash INSTALL_ALL.sh
curl http://localhost:8002/health
curl http://localhost:8002/ready
systemctl status webitgpt --no-pager
systemctl status webitgpt-mongo --no-pager
```

## 注意事項

- 這是完整離線包，不是 patch；適合新主機第一次安裝。
- 目標機不需要上網。
- 正式包不帶目前家中測試資料；安裝後可用 CMDB CSV/Excel 匯入正式主機清單。
