# v1.0.3.91 - asset-list-row-number-first-column

## 變更內容

- 資產管理清單第一欄由 `SN` 改為 `序號`。
- 清單第一欄顯示 1、2、3 這類簡單列號，降低大量資產掃讀負擔。
- 資產流水號仍保留在資料、詳情與編輯流程中，不會遺失追溯資訊。

## 驗證

- `python -m pytest tests/test_ui_contracts.py -q`
- `python -m pytest tests/test_asset_account_ui.py -q`
- `python -m pytest -q`

## 安裝

GitHub Release patch 套件內固定使用：

```bash
bash install.sh
```
