# v1.0.4.1 - cmdb-relationship-row-action-menu

## Changes

- Changed the CMDB relationship coverage detail table action column from a direct text link to a compact per-row dropdown menu.
- Kept the "View impact map" action available, while reducing table row height and visual noise.
- Added UI contract coverage so the row action menu does not regress back to a space-consuming link.

## Verification

- `python -m pytest tests/test_asset_account_ui.py::test_cmdb_relationship_dashboard_contracts_exist tests/test_asset_account_ui.py::test_cmdb_relationship_page_renders_with_overview -q`
- `python -m pytest -q`

## Field Note

This patch changes only the CMDB relationship coverage UI. It does not modify CMDB data, relationship data, or topology calculations.
