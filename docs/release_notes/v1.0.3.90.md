# v1.0.3.90 asset-list-sn-first-column

## 變更內容

- 資產管理清單新增 `SN` 第一欄，優先顯示資產編號 / 資產流水號。
- `Hostname` 欄位不再同時塞入資產編號，只保留主機名稱與驗證狀態。
- 調整資產清單欄寬，讓 SN、資產狀態、資產名稱與 Hostname 更容易橫向掃讀。

## 驗證

- `python -m pytest tests/test_ui_contracts.py::test_asset_management_page_exposes_governance_workflow -q`
- `python -m pytest tests/test_asset_account_ui.py -q`
- `python -m pytest -q`

## 注意

- 本版只調整資產管理清單顯示順序，不改資料模型、不搬資料、不改匯入邏輯。
