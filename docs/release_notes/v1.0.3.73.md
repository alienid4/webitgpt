# v1.0.3.73 - asset-status-first-column

## 重點

- 資產管理列表新增第一欄「資產狀態」。
- 使用顏色標籤直接區分使用中、草稿、待處理、停用與已汰除。
- Hostname / OS 旁的「未驗證」保留為採證狀態，不再與資產生命週期混淆。
- 表格寬度與展開明細列同步調整，避免新增欄位後錯位。

## 驗證

- `python -m compileall webapp`
- `python -m pytest -q`
- 221 測試主機檢查 `/health` 與 `/hosts`
