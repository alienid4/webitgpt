# v1.0.3.88 opening-scope-help-compact-ui

## 重點

- 開門檢查的「範圍說明」改成問號提示，不再佔用篩選表單空間。
- 說明內容仍保留：正式資產總數、可納入開門檢查的 Linux / Windows / AIX / AS400，以及端點、網路設備、VMware host 不跑 L1 主機巡檢的原因。
- 修正窄版或遠端桌面解析度下，提示文字變成直排並擠壓統計卡片的問題。

## 驗證

- `python -m pytest tests/test_asset_account_ui.py -q`
- `python -m pytest -q`
