# v1.0.3.80 data-quality-bulk-actions-by-issue-type field commands

## 目的

驗證資料品質工作台是否已支援同類型批次處理，並確認版本與資料品質統計正常。

## 安裝

```bash
cd /tmp
sha256sum -c webitgpt_v1.0.3.80-patch-data-quality-bulk-actions-by-issue-type_202606012345.tar.gz.sha256
tar -xzf webitgpt_v1.0.3.80-patch-data-quality-bulk-actions-by-issue-type_202606012345.tar.gz
cd webitgpt_v1.0.3.80-patch-data-quality-bulk-actions-by-issue-type_202606012345
WEBITGPT_PIP_MODE=offline bash install.sh
```

## 驗證

這段輸出已去敏，只顯示版本、狀態與統計數量。

```bash
python3 - <<'PY'
import json
import urllib.request

BASE = "http://127.0.0.1:8002"

def get_json(path):
    with urllib.request.urlopen(BASE + path, timeout=15) as res:
        return json.loads(res.read().decode("utf-8"))

health = get_json("/health")
quality = get_json("/api/reports/data-quality")
summary = quality.get("summary", {})

print(json.dumps({
    "version": health.get("version"),
    "patch_id": health.get("patch_id"),
    "health_status": health.get("status"),
    "quality_status": quality.get("status"),
    "platform_mismatch_count": summary.get("cmdb_platform_mismatch"),
    "missing_connection_count": summary.get("cmdb_missing_connection"),
    "workflow_pending_count": summary.get("cmdb_workflow_pending")
}, ensure_ascii=False, indent=2, sort_keys=True))
PY
```

## 操作口徑

- 平台分類與缺少連線方式屬於可安全規則化項目，可在資料品質工作台同類型批次處理。
- 草稿與流程未結案屬於人工治理項目，系統只導向批次治理流程，不自動全部轉正式。
