# v1.0.3.78 cmdb-import-os-platform-auto-classify 現場指令

以下指令只輸出版本、HTTP 狀態、數量與雜湊，不輸出主機名稱、IP、資產編號、系統名稱或人名。

## 安裝

```bash
curl -LO https://github.com/alienid4/webitgpt/releases/download/v1.0.3.78/webitgpt_v1.0.3.78-patch-cmdb-import-os-platform-auto-classify_202606012235.tar.gz
curl -LO https://github.com/alienid4/webitgpt/releases/download/v1.0.3.78/webitgpt_v1.0.3.78-patch-cmdb-import-os-platform-auto-classify_202606012235.tar.gz.sha256
sha256sum -c webitgpt_v1.0.3.78-patch-cmdb-import-os-platform-auto-classify_202606012235.tar.gz.sha256
tar -xzf webitgpt_v1.0.3.78-patch-cmdb-import-os-platform-auto-classify_202606012235.tar.gz
cd webitgpt_v1.0.3.78-patch-cmdb-import-os-platform-auto-classify_202606012235
WEBITGPT_PIP_MODE=offline WEBITGPT_BUILD_TIME='2026-06-01 22:35:00 +08:00' bash install.sh
```

## 驗證

```bash
python3 - <<'PY'
import json
import urllib.request

base = "http://127.0.0.1:8002"

def get_json(path):
    with urllib.request.urlopen(base + path, timeout=15) as res:
        return json.loads(res.read().decode("utf-8"))

health = get_json("/health")
quality = get_json("/api/reports/data-quality")

summary = {
    "version_ok": health.get("version") == "1.0.3.78",
    "patch_id": health.get("patch_id"),
    "health_status": health.get("status"),
    "quality_status": quality.get("status"),
    "platform_mismatch_count": quality.get("summary", {}).get("cmdb_platform_mismatch"),
}
print(json.dumps(summary, ensure_ascii=False, indent=2, sort_keys=True))
PY
```
