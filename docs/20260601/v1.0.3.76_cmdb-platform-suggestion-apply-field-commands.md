# v1.0.3.76 cmdb-platform-suggestion-apply 現場指令

用途：資料品質頁面已能找出 OS 與平台分類不一致的資產。本版新增管理員可執行的「套用平台分類建議」，把明確可判斷的 Linux / Windows / AIX / AS400 / VMware 平台寫回資產主檔。

## 下載與安裝

```bash
cd /tmp
curl -LO https://github.com/alienid4/webitgpt/releases/download/v1.0.3.76/webitgpt_v1.0.3.76-patch-cmdb-platform-suggestion-apply_202606011910.tar.gz
curl -LO https://github.com/alienid4/webitgpt/releases/download/v1.0.3.76/webitgpt_v1.0.3.76-patch-cmdb-platform-suggestion-apply_202606011910.tar.gz.sha256
sha256sum -c webitgpt_v1.0.3.76-patch-cmdb-platform-suggestion-apply_202606011910.tar.gz.sha256
tar -xzf webitgpt_v1.0.3.76-patch-cmdb-platform-suggestion-apply_202606011910.tar.gz
cd webitgpt_v1.0.3.76-patch-cmdb-platform-suggestion-apply_202606011910
WEBITGPT_PIP_MODE=offline WEBITGPT_BUILD_TIME='2026-06-01 19:10:00 +08:00' bash install.sh
```

## 驗證

```bash
python3 - <<'PY'
import json
import urllib.request

base = "http://127.0.0.1:8002"

with urllib.request.urlopen(base + "/health", timeout=5) as res:
    health = json.loads(res.read().decode("utf-8"))

with urllib.request.urlopen(base + "/api/reports/data-quality", timeout=10) as res:
    quality = json.loads(res.read().decode("utf-8"))

print(json.dumps({
    "version_ok": health.get("version") == "1.0.3.76",
    "patch_id": health.get("patch_id"),
    "quality_status": quality.get("status"),
    "cmdb_issues": (quality.get("summary") or {}).get("cmdb_issues"),
    "platform_mismatch": (quality.get("summary") or {}).get("cmdb_platform_mismatch")
}, ensure_ascii=False, sort_keys=True))
PY
```

## 套用平台分類建議

1. 開啟 `/reports/data-quality`。
2. 如果「平台分類疑似錯誤」大於 0，按「套用平台分類建議」。
3. 回到主管儀表板或資產管理頁確認平台統計，Linux / Windows / AIX 等數量應依 OS 判讀後更新。

注意：這個動作只修正可由 OS 明確推論的平台分類，不會修改 IP、Hostname、owner、APID 或核心關聯。
